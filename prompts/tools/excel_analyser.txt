你的任务是先分析，再生成代码，生成的代码应该完成以下两个目标：
（1）利用Pandas库分析表格并获取完成最终任务所需的所有数据，必要时进行计算
（2）将所获取的数据存储到csv文件中，存储的目标文件为'output/FILENAME.csv'，其中FILENAME需要你根据具体情况起一个合适的文件名
注意：FILENAME.csv文件存储到'./output'文件夹中去

请根据用户的输入，一步步分析：
（1）用户的输入是否依赖某个条件，而这个条件没有明确赋值？
（2）我是否需要对某个变量的值做假设？

如果我需要对某个变量的值做假设，请直接输出：
```python
print("我需要知道____的值，才能生成代码。请完善你的查询。") # 请将____替换为需要假设的的条件
```

否则，生成一段Python代码，完成上述两个目标

你可以使用的库只包括：Pandas, re, math, datetime
确保你的代码只使用上述库，否则你的代码将无法运行。

给定文件为：
{filename}

文件内容样例：
{inspections}

你将会用到以下知识：
{knowledge}

你输出的Python代码前后必须有markdown标识符，如下所示：
```python
# example code
print('hello world')
```

确保你的代码是可以运行的，文件名直接写死在代码里即可。
你生成代码中所有的常量都必须来自我给你的信息或来自文件本身。不要编造任何常量。
如果常量缺失，你的代码将无法运行。你可以拒绝生成代码，但是不要生成编造的代码。

确保你生成的代码最终以print的方式输出结果，要求：
（1）用print回答用户的问题。
（2）如果代码中产生了csv等类型的文件，用print输出所产生文件的路径，供后续步骤使用。

用户输入：
{query}
